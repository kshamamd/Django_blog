from django.apps import AppConfig


class BossConfig(AppConfig):
    name = 'boss'
#profile img at the corner on the list view of the post then
#at the separate view of the post bottom border and there redirect comments on the remaining page?
#links or button or icons for remaining views---done
#apply very nice bootstrap
####like button toggle and counter
#new date should be there when updated